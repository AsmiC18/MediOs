// Determines Growth/Pro/Enterprise plan-gated features; shows disabled/upsell state.
export default function usePlanGate(featureKey) {
  // TODO: implement once backend plan/entitlement data is available
  return { isLocked: false, isUpsell: false };
}
